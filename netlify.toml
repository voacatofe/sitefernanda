[build]
  command = "pnpm build"
  publish = "out"
  
[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--version" # Impede que o Netlify tente instalar dependências novamente

# Redirecionamentos para o SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 